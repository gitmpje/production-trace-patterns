services:
  event-data-base:
    image: stain/jena-fuseki
    container_name: event-data-base
    ports:
      - 3030:3030
    environment:
      - FUSEKI_DATASET_1=automotive
      - ADMIN_PASSWORD=admin
